/**
 * Hooks Index
 *
 * Re-exports all custom hooks for cleaner imports.
 * Usage: import { useSocket, useLeaderboard } from './hooks';
 */

export { useSocket } from './useSocket';
export { useLeaderboard } from './useLeaderboard';
